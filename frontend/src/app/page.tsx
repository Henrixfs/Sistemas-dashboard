import { AppShell } from '@/components/AppShell';

export default function HomePage() {
  return (
    <AppShell>
      <section aria-labelledby="page-title">
        <h1 id="page-title">EPIS Transparente</h1>
        <p>La plataforma de transparencia financiera se encuentra en preparación.</p>
      </section>
    </AppShell>
  );
}
